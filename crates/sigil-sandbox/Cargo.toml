[package]
name = "sigil-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
async-trait.workspace = true
sigil-kernel = { path = "../sigil-kernel" }
sigil-resource-authority = { path = "../sigil-resource-authority" }
sigil-process = { path = "../sigil-process" }
serde.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio.workspace = true

[target.'cfg(unix)'.dependencies]
portable-pty = "=0.9.0"

[target.'cfg(windows)'.dependencies]
portable-pty = "=0.8.1"

[dev-dependencies]
futures.workspace = true
sigil-resource-authority = { path = "../sigil-resource-authority", features = ["test-support"] }
tempfile.workspace = true
serde_json.workspace = true

[lints]
workspace = true
