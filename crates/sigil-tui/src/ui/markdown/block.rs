//! Block projection and presentation helpers live behind the `ui::markdown`
//! façade. CommonMark block rendering remains in the façade while bounded
//! normalization, math, and Mermaid behavior are isolated in sibling modules.
