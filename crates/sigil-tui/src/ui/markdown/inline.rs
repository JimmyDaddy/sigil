//! Inline Markdown presentation helpers live behind the `ui::markdown` façade.
//!
//! The first V1 extraction keeps the public renderer stable while math-specific
//! delimiter handling moves into `math.rs`.
