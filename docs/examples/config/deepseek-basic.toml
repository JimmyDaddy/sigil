[workspace]
root = "."

[agent]
provider = "deepseek"
model = "deepseek-v4-flash"
tool_timeout_secs = 30

[permission]
mode = "manual"

[providers.deepseek]
# Prefer SIGIL_API_KEY in your shell.
# api_key = "sk-..."
