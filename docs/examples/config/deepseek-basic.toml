[workspace]
root = "."

[agent]
provider = "deepseek"
model = "deepseek-v4-flash"
tool_timeout_secs = 30

[permission]
preset = "balanced"
default_mode = "ask"

[permission.access]
read = "allow"

[providers.deepseek]
model = "deepseek-v4-flash"
# Prefer SIGIL_API_KEY in your shell.
# api_key = "sk-..."
